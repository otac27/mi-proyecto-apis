
server.port=8085

# Configuración de la Base de Datos (DataSource)
spring.datasource.url=jdbc:mysql://localhost:3306/forohub
spring.datasource.username=root
spring.datasource.password=OMiguel35+

# Configuración de JPA/Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Desactiva Open-in-View para mejor rendimiento en APIs REST
spring.jpa.open-in-view=false

# Configuración de Flyway
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate

# Secreto para la firma de tokens JWT (en un proyecto real, esto debería ser una variable de entorno)
api.security.secret=mi_super_secreto_12345